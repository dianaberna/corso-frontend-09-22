const message = () => {
    const name = "Jesse";
    const age = 40;
    return name + ' is ' + age + 'years old.';
};

function miaFunzione(){
    const name = "Jesse";
    const age = 40;
    return name + ' is ' + age + 'years old.';
}

const message2 = miaFunzione();